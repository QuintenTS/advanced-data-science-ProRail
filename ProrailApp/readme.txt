Set up a virtual environment with "requirements.txt"
Copy and paste the lines below in the bash.
Change the path to where you put the folder and the virtual environment.

cd /d "E:\ProrailApp"
.\.venv\Scripts\activate
streamlit run app.py
